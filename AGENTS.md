# Agent Instructions

Before working in this repository, read the shared guidance in [.agent-rules.md](.agent-rules.md). It is the common entry point for Codex, Cursor, and Claude and links to the authoritative rules under `.agent-rules/`.

For repository architecture, build commands, test conventions, and project-specific contracts, also read [CLAUDE.md](CLAUDE.md). Load matching skills from `.agents/skills/` only when their workflow applies.
