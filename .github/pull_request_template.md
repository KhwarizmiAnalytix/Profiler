## Summary

<!-- What does this change, and why? -->

## Test plan

<!-- How did you verify this? e.g.:
- [ ] `ctest --test-dir build -C Release --output-on-failure`
- [ ] Ran under `-DPROFILER_SANITIZER=address,undefined` (if touching concurrency/lifetime code)
- [ ] Updated/added tests in `Testing/Cxx/`
-->

## Checklist

- [ ] `CHANGELOG.md` updated under `## Unreleased` (if user-visible)
- [ ] `docs/` updated (if behavior, build options, or public API changed)
- [ ] `lintrunner init && lintrunner` clean on changed files
